{
  "name": "me-api",
  "version": "1.0.0",
  "private": true,
  "main": "chat/index.js",
  "author": "Lodovico Minnocci",
  "license": "MIT",
  "dependencies": {
    "mssql": "^12.2.1"
  },
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "coverage": "vitest run --config=vitest.config.js --coverage",
    "test:vitest": "vitest run --config=vitest.config.js",
    "lint": "npx eslint '**/*.js' --ignore-pattern '**/__tests__/**' --ignore-pattern '**/*.test.*'",
    "lint:fix": "npx eslint '**/*.js' --ignore-pattern '**/__tests__/**' --ignore-pattern '**/*.test.*' --fix"
  },
  "devDependencies": {
    "@vitest/coverage-istanbul": "^4.1.5",
    "vitest": "^4.1.5"
  }
}
